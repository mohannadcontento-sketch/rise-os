/**
 * @deprecated This file is kept for backward compatibility only.
 * All API routes now use Supabase directly via @/lib/supabase.
 * SQLite/Prisma is NO LONGER used on the server (Vercel/online).
 *
 * Offline/PWA mode uses IndexedDB via @/lib/offline-db.ts (client-side only).
 */